<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".PersonalPage.ProfileFragment">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <Button
            android:id="@+id/createProjectBtn"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_alignParentEnd="true"
            android:layout_alignParentBottom="true"
            android:layout_marginEnd="24dp"
            android:layout_marginBottom="24dp"
            android:background="@drawable/primary_button"
            android:textColor="@color/white"
            android:text="+"
            android:textSize="32sp"
            />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <include
                android:id="@+id/personalPageAvatar"
                layout="@layout/person_avatar" />

            <HorizontalScrollView
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:orientation="horizontal">

                    <Button
                        android:id="@+id/activeProjectsBtn"
                        android:layout_width="150dp"
                        android:layout_height="wrap_content"
                        android:backgroundTint="@color/white"
                        android:text="Активные"
                        android:textAllCaps="false" />

                    <Button
                        android:id="@+id/portfolioBtn"
                        android:layout_width="150dp"
                        android:layout_height="wrap_content"
                        android:backgroundTint="@color/white"
                        android:text="Портфолио"
                        android:textAllCaps="false" />

                    <Button
                        android:id="@+id/collaborationBtn"
                        android:layout_width="150dp"
                        android:layout_height="wrap_content"
                        android:backgroundTint="@color/white"
                        android:text="Коллаборация"
                        android:textAllCaps="false" />

                    <Button
                        android:id="@+id/favoritesBtn"
                        android:layout_width="150dp"
                        android:layout_height="wrap_content"
                        android:backgroundTint="@color/white"
                        android:text="Избранное"
                        android:textAllCaps="false" />
                </LinearLayout>
            </HorizontalScrollView>


            <Button
                android:id="@+id/addPortfolioBtn"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="8dp"
                android:layout_marginTop="8dp"
                android:layout_marginStart="24dp"
                android:layout_marginEnd="24dp"
                android:background="@drawable/secondary_button"
                android:textColor="@color/purple"
                android:text="+"
                android:visibility="gone"
                android:textSize="36sp" />

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/projectsView"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />

        </LinearLayout>

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbarPersonalUserPage"
            android:layout_width="match_parent"
            android:layout_height="36dp"
            app:popupTheme="@style/AppTheme.PopupMenu"
            android:background="@color/transparent"
            app:theme="@style/AppTheme.Toolbar"
            />
    </RelativeLayout>

</FrameLayout>